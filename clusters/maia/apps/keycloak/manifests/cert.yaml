apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-tls
  namespace: keycloak
spec:
  secretName: keycloak-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  commonName: keycloak.sleepy-puppy.com
  dnsNames:
    - keycloak.sleepy-puppy.com

