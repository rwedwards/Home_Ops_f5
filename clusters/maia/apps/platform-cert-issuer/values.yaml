groupName: acme.sleepy-puppy.com

namecheap:
  apiUser:
    valueFrom:
      secretKeyRef:
        name: namecheap-api-key
        key: apiUser
  apiKey:
    valueFrom:
      secretKeyRef:
        name: namecheap-api-key
        key: apiKey

# --- DNS override for the webhook pod ---
dnsPolicy: None
dnsConfig:
  nameservers:
    - 1.1.1.1
    - 8.8.8.8
